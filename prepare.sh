# Create documents directory
mkdir documents

# Create db directory
mkfir db

# # Create symlink for .env (need for prisma)
# ln -sf .env.dev .env

# # Run migrations
# npx prisma migrate dev --name init
